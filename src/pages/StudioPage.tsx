import React, { FC } from 'react';
import { App } from '../App';

export const StudioPage: FC = () => {
    // The Studio page is the main app itself
    // This is just a wrapper component
    return <App />;
};
